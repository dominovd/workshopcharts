{
  "name": "workshopcharts",
  "private": true,
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "build": "cd site && npm ci && npm run build",
    "dev": "cd site && npm run dev",
    "check": "cd site && npm run check:data"
  }
}
