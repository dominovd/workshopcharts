{
  "name": "workshopcharts",
  "private": true,
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "build": "npm --prefix site run build",
    "dev": "npm --prefix site run dev",
    "check": "npm --prefix site run check:data"
  }
}
